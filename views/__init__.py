"""Views package — Streamlit page modules for the Connection Explorer.

Each module exposes a ``run()`` function that is registered as a
``st.Page`` in the main application's ``st.navigation`` call.
"""
